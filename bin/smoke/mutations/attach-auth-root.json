{
  "suite": [
    "bin/smoke/attach-auth-root.smoke.ts"
  ],
  "guard": "attach redeems a session grant with the seed the MESH resolved, never one walked up from the cwd, and a cwd anchor holding another chain for the same space is reported rather than obeyed",
  "command": "pnpm --filter @cotal-ai/workspace build && pnpm --filter @cotal-ai/cli build && pnpm smoke:attach-auth-root",
  "executes": ["bin/cotal.ts"],
  "completionMarker": "attach auth-root:",
  "proveWith": "node scripts/mutation-proof.mjs --config bin/smoke/mutations/attach-auth-root.json",
  "why": [
    "EVERY ATTACH CELL RUNS THE BINARY, so the command builds first. The suite drives `tsx",
    "bin/cotal.ts` and `@cotal-ai/cli` resolves to `dist/`, so a mutation on `src` that is not built",
    "is a mutation the run never sees: it would come back SURVIVED and read as `this line does not",
    "matter` when what was measured is `this line was not in the artifact`. `@cotal-ai/workspace`",
    "builds first because the CLI's dist consumes it, and mutations 3 and 4 live there.",
    "",
    "MUTATION 1 IS THE DEFECT ITSELF, restored verbatim rather than approximated: the pre-fix line",
    "asked the CURRENT DIRECTORY which root holds the seed, after resolution had already answered",
    "that question and connected with its answer. It is written with local aliases only because the",
    "fix deleted those three imports from the file; the expression is the one that shipped.",
    "",
    "WHAT MUTATION 1 PROVES AND WHAT IT DOES NOT. It proves the fossil cell REACHES the redemption",
    "seed and depends on which chain it comes from -- the credential is minted from the cwd's dead",
    "chain and the broker rejects it. It does not prove the cell would catch a defect anywhere else",
    "on the attach path, and nothing here claims that.",
    "",
    "MUTATION 2 IS THE OTHER HALF OF THE FIX AND IS NOT REDUNDANT WITH 1. Carrying the resolution's",
    "seed forward makes attach CORRECT; reporting the divergent anchor makes it LEGIBLE. Delete the",
    "report and every functional cell in this suite still passes, which is exactly the state issue",
    "#722 was found in: the command worked or failed for a reason the operator had no way to see.",
    "",
    "MUTATIONS 3 AND 4 ARE THE DETECTOR'S TWO FALSE DIRECTIONS, one each. 3 makes it blind whenever",
    "the resolved root also holds a chain (a false NEGATIVE: the divergent anchor stops being",
    "reported). 4 makes it fire from any directory that merely lacks auth (a false POSITIVE: every",
    "unanchored directory grows a warning about a trust chain that does not exist). A detector is",
    "only worth its output if BOTH directions are held, and a suite that pinned one and not the",
    "other would let the other regress silently.",
    "",
    "NOT GRADED, and said rather than implied: the arm of the no-seed refusal for a connection with",
    "no resolved root. No supported route reaches redemption in that state -- both off-registry",
    "routes are refused earlier, which the suite MEASURES in cells D -- so there is no mutation for",
    "it here. An unreachable arm cannot be killed, and a mutation aimed at one would report",
    "SURVIVED and read as a coverage gap rather than as the invariant it actually is.",
    "",
    "MUTATIONS 5 AND 6 ARE THE SECOND ANSWER IN ITS OTHER FORM. Carrying the resolution's seed forward",
    "stops the cwd choosing WHICH chain is used; it does not stop the cwd ending the command. The",
    "detector reads the walked root before the mint, and `loadSpaceAuth` refuses unreadable trust",
    "material loudly, so a corrupt `broker.json` up the walk aborted an attach that had already",
    "declared it was not using that root -- and on the reconnect path that abort is classified",
    "transient and retried forever. 5 restores that shape whole and is graded through the binary. 6 is",
    "the half fix, guarding only the side the bug report named, and it is not redundant: an unreadable",
    "RESOLVED root leaves nothing to compare against, and reporting a divergence from it would be a",
    "claim this code cannot support.",
    "",
    "THE ONE INPUT THAT IS NOT GRADED, and said rather than implied: a process whose working directory",
    "has been DELETED. `findCotalRoot` defaults its argument to `process.cwd()`, which throws on that",
    "input, which is why the call sits inside the guard rather than before it. There is no mutation and",
    "no cell for it because the only way to produce it is to chdir into a directory and remove it, and",
    "this suite process goes on to read fixtures and spawn the real binary afterwards: a cell that",
    "sabotages its own process's cwd would take the other twenty four with it. An explicit path cannot",
    "stand in, since a non-existent PATH walks to the filesystem root and returns normally. The guard is",
    "structural instead, and the contract comment states the bound."
  ],
  "mutations": [
    {
      "name": "the defect restored: redemption walks up from the cwd for the seed instead of using the one the mesh resolved",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "  const auth = t.spaceAuth;",
      "replace": "  const { authDir: mAuthDir, findCotalRoot: mFind, loadSpaceAuth: mLoad } = await import(\"@cotal-ai/workspace\");\n  const auth = mLoad(mAuthDir(mFind()), t.space);",
      "expectRed": "attach from a fossil-anchored directory redeems and opens the session",
      "cell": "attach from a fossil-anchored directory redeems and opens the session",
      "afterRestore": "pnpm --filter @cotal-ai/cli build",
      "note": "This is issue #722 verbatim. The cwd holds a fossil `.cotal` whose chain the broker does not trust, so the session-caller credential is minted from it and the connect is refused."
    },
    {
      "name": "the divergent-anchor report is deleted, leaving the command correct and the operator blind",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "  if (shadow)\n    console.error(\n      `! this directory resolves to ${shadow.cwdRoot}, whose .cotal/auth holds a DIFFERENT trust chain for space \"${t.space}\".\\n` +\n      `  attach used ${t.root}, the root this mesh resolved to. The other one is not being used, and is worth a look.`,\n    );",
      "replace": "  void shadow;",
      "expectRed": "…and the shadowing anchor is REPORTED, naming the walked root and the one actually used",
      "cell": "…and the shadowing anchor is REPORTED, naming the walked root and the one actually used",
      "afterRestore": "pnpm --filter @cotal-ai/cli build",
      "note": "A careless edit, not a malicious one: the report is dropped and the detector call kept. Everything still typechecks and every functional cell still passes."
    },
    {
      "name": "the detector goes blind whenever the resolved root also holds a chain (false negative)",
      "file": "packages/workspace/src/auth-paths.ts",
      "find": "  if (there && here.account.pub === there.account.pub) return undefined;",
      "replace": "  if (there) return undefined;",
      "expectRed": "a cwd anchor carrying a DIFFERENT chain for the space is detected, and names that root",
      "cell": "a cwd anchor carrying a DIFFERENT chain for the space is detected, and names that root",
      "afterRestore": "pnpm --filter @cotal-ai/workspace build && pnpm --filter @cotal-ai/cli build",
      "note": "The resolved root holds a chain in every real mesh, so this silences the detector everywhere it matters while leaving it green on the cells where nothing should be reported."
    },
    {
      "name": "the detector fires from any directory that merely lacks auth (false positive)",
      "file": "packages/workspace/src/auth-paths.ts",
      "find": "  if (!here) return undefined;",
      "replace": "  if (!here) return { cwdRoot };",
      "expectRed": "standing where there is no anchor at all says nothing",
      "cell": "standing where there is no anchor at all says nothing",
      "afterRestore": "pnpm --filter @cotal-ai/workspace build && pnpm --filter @cotal-ai/cli build",
      "note": "Turns the report into noise: every directory outside a checkout would warn about a trust chain it does not have, which is how a real signal gets trained away."
    },
    {
      "name": "the detector can throw again: an unreadable root on the way past aborts a command that is not using it",
      "file": "packages/workspace/src/auth-paths.ts",
      "find": "  let cwdRoot: string;\n  let here: SpaceAuth | undefined;\n  let there: SpaceAuth | undefined;\n  try {\n    cwdRoot = findCotalRoot(cwd);\n    if (resolve(cwdRoot) === resolve(resolvedRoot)) return undefined;\n    here = loadSpaceAuth(authDir(cwdRoot), space);\n    there = here ? loadSpaceAuth(authDir(resolvedRoot), space) : undefined;\n  } catch {\n    return undefined;\n  }\n  if (!here) return undefined;",
      "replace": "  const cwdRoot = findCotalRoot(cwd);\n  if (resolve(cwdRoot) === resolve(resolvedRoot)) return undefined;\n  const here = loadSpaceAuth(authDir(cwdRoot), space);\n  if (!here) return undefined;\n  const there = loadSpaceAuth(authDir(resolvedRoot), space);",
      "expectRed": "attach from a directory whose ancestor auth is CORRUPT still opens the session",
      "cell": "attach from a directory whose ancestor auth is CORRUPT still opens the session",
      "afterRestore": "pnpm --filter @cotal-ai/workspace build && pnpm --filter @cotal-ai/cli build",
      "note": "The shape the first fix shipped in. `loadSpaceAuth` throws on trust material it cannot read, so a half-written `broker.json` anywhere up the cwd walk kills an attach whose seed came from the resolved root. Graded through the REAL binary, because that is where the abort is fatal."
    },
    {
      "name": "only the walked root is guarded, so an unreadable RESOLVED root still throws (the half fix)",
      "file": "packages/workspace/src/auth-paths.ts",
      "find": "  let cwdRoot: string;\n  let here: SpaceAuth | undefined;\n  let there: SpaceAuth | undefined;\n  try {\n    cwdRoot = findCotalRoot(cwd);\n    if (resolve(cwdRoot) === resolve(resolvedRoot)) return undefined;\n    here = loadSpaceAuth(authDir(cwdRoot), space);\n    there = here ? loadSpaceAuth(authDir(resolvedRoot), space) : undefined;\n  } catch {\n    return undefined;\n  }\n  if (!here) return undefined;",
      "replace": "  const cwdRoot = findCotalRoot(cwd);\n  if (resolve(cwdRoot) === resolve(resolvedRoot)) return undefined;\n  let here: SpaceAuth | undefined;\n  try {\n    here = loadSpaceAuth(authDir(cwdRoot), space);\n  } catch {\n    return undefined;\n  }\n  if (!here) return undefined;\n  const there = loadSpaceAuth(authDir(resolvedRoot), space);",
      "expectRed": "…and so does an unreadable RESOLVED root, rather than claiming a divergence it cannot see",
      "cell": "…and so does an unreadable RESOLVED root, rather than claiming a divergence it cannot see",
      "afterRestore": "pnpm --filter @cotal-ai/workspace build && pnpm --filter @cotal-ai/cli build",
      "note": "The fix someone writes after reading only the bug report: guard the side the report named. The other side is the same abort with the roots swapped, and it is the direction a later change is most likely to reintroduce."
    },
    {
      "name": "CONTROL the attach seed-output assertion is live in its suite file",
      "file": "bin/smoke/attach-auth-root.smoke.ts",
      "find": "    ![fromFossil, fromBare, fromCorrupt].some((o) =>\n      /(?:^|\\n)(?:✓ added @cotal-ai\\/|→ wrote operator-global seed store payload|✗ refusing to reconcile the operator-global seed store)/.test(o),\n    ),",
      "replace": "    false,",
      "expectRed": "the attached-session subprocess does not reconcile connector payloads before exercising attach",
      "cell": "the attached-session subprocess does not reconcile connector payloads before exercising attach",
      "note": "Positive control for the same-file reach requirement. It proves the suite executes the seed-output assertion before the seed-skip mutation is graded."
    },
    {
      "name": "the attached-session subprocess loses the seed-skip guard and reconciles before attach",
      "file": "bin/smoke/attach-auth-root.smoke.ts",
      "find": "cwd,\n    env: { ...cleanEnv, COTAL_HOME: home, XDG_CONFIG_HOME: join(home, \"xdg\"), COTAL_SKIP_CONNECTOR_SEED: \"1\", NO_COLOR: \"1\" },\n    stdio:",
      "replace": "cwd,\n    env: { ...cleanEnv, COTAL_HOME: home, XDG_CONFIG_HOME: join(home, \"xdg\"), NO_COLOR: \"1\" },\n    stdio:",
      "expectRed": "the attached-session subprocess does not reconcile connector payloads before exercising attach",
      "cell": "the attached-session subprocess does not reconcile connector payloads before exercising attach",
      "note": "The attach subprocess must reach the attach path directly. Without the skip, the command auto-reconciles connector payloads first; its seed output can consume the bounded window and hide both the attached banner and the divergent-root report."
    }
  ]
}
