{
  "suite": [
    "implementations/manager/smoke/attach-stdin.smoke.ts"
  ],
  "guard": "nothing typed at a terminal with no session reaches the agent, and the detach key still works there",
  "command": "pnpm --filter @cotal-ai/cli build && pnpm smoke:attach-stdin",
  "executes": ["bin/cotal.ts"],
  "completionMarker": "O: the manager is back on the session count it started with",
  "proveWith": "node scripts/mutation-proof.mjs --config bin/smoke/mutations/attach-stdin.json",
  "why": [
    "EVERY CELL HERE RUNS THE BINARY, so every command builds first. The suite drives `tsx",
    "bin/cotal.ts`, and `@cotal-ai/cli` resolves to `dist/`, so a mutation on `src` that is not built",
    "is a mutation the run never sees: it would come back SURVIVED and read as `this line does not",
    "matter` when what was measured is `this line was not in the artifact`. `dist/` is gitignored, so",
    "git cannot be the recovery for it either, hence the rebuild in every `afterRestore`.",
    "",
    "THE SET IS AIMED AT THE WITNESS CELLS, AND THERE IS ONLY ONE BOUNDARY LEFT. Cell A (typed during",
    "a WAIT) passes with this whole change reverted: the old watcher owned the wait, so what was",
    "typed there was read and dropped then too. Nothing here is aimed at it. Cell B was filed as a",
    "second boundary and was not one. Its premise, that the attempt had FAILED, was asserted in a",
    "comment and never checked, and an establishment on a held link runs to the 5s link deadline",
    "(dials measured over a 20s hold: 0, 5039, 5040, 6041, 6041, 16047), so the cell's 2.5s hold",
    "completed the attempt still in flight instead of following a failed one. With the premise",
    "enforced by the client's own re-dial, a revert delivers the nonce to the seat, so B is a witness",
    "and has its own mutation here. Cells J and L are the piped population, and two mutations are",
    "aimed at them: one saying the gate must be exactly `isTTY` rather than absent, and one saying it",
    "must hold in EVERY window rather than only at the first attach.",
    "",
    "TWO MUTATIONS CARRY THEIR OWN COMPLETION MARKER, and the reason is the finding rather than a",
    "workaround. The global marker is the last cell's trailer. Reverting the fix does not",
    "merely redden a check: under it cell D's Ctrl-C reaches the seat as a real SIGINT and kills the",
    "agent, cell E then cannot resolve the seat at all, and the suite stops there. So no mutation",
    "that restores the shipped defect can reach the last cell, by construction, and the harness",
    "correctly refuses to grade an unfinished run. The reversion and the wait-reader mutation name",
    "their own cell's trailer instead, which both of them print. The unreachability is information:",
    "the harm this change closes arrives as a side effect of measuring it.",
    "",
    "THE POPULATION GATE HAS ONE HOME, and mutation 11 is the reason it needed one. It was written at",
    "the first-attach call site, and the loop has two more: the backoff and the session hand-back. So",
    "a piped attach buffered correctly until its first reconnect and then had a reader installed on",
    "it by the backoff, which read the script's bytes and dropped them. That contradicts the contract",
    "this same change wrote into docs/cli.md, and it contradicts what the code did BEFORE the change,",
    "which paused stdin between sessions and let the next `resume()` flush them. Found as a",
    "reasoned-only footnote under an APPROVE, reproduced before it was fixed (62 passed, 1 failed,",
    "the single red cell L's delivery check with the seat's read empty), then fixed by moving the",
    "gate inside `ownStdin` so it cannot be honoured at one site and skipped at the next. Mutation 11",
    "restores exactly the shipped shape, gate at the first attach and nowhere after: J stays green",
    "and L reddens, which is the pair that names the defect.",
    "",
    "AN OBLIGATION THAT LIVES AT SOME CALL SITES IS NOT AN OBLIGATION, and this file now records the",
    "same defect twice in two different shapes. The population gate was written at one `ownStdin`",
    "call site of three, so a piped attach gained a byte-dropper the moment it reconnected (mutation",
    "11). Giving the stream BACK was hiding in the same optional path: the pause that released it",
    "lived inside the watcher's `stop`, so releasing was accidentally conditional on ever having",
    "taken it. Honour the gate everywhere and there is no watcher to stop, so nothing released the",
    "stream and the command sat there having already printed `detached from`, which is the third",
    "instance in this lane of one signature: the attach says it detached and the shell does not come",
    "back (the abandoned backoff timer and the late-landing session's link were the first two).",
    "Measured on the lingering process rather than argued: an unref'd-timer preload printing",
    "`getActiveResourcesInfo()` reported three PipeWraps, no timers and no sockets. A pause alone was",
    "tried first and measured as insufficient, so that line was removed rather than kept with a",
    "comment claiming a fix it did not deliver. Mutation 12 grades the replacement. It is NOT the",
    "attribution: no mutation of this tree can say what another build does, and \"pre-existing\" is a",
    "claim about the build that ships. That half has its own instrument, `pnpm probe:pipe-oneshot`,",
    "which is the same trigger with the assertions stripped and the worktree as the arm: at",
    "origin/main (a8589c11) it reports exit=HANG ms=>30000 with the detach honoured and `detached",
    "from` printed; at this tip, exit=0 ms=102. Two sentences, two instruments.",
    "",
    "RUN, NOT PREDICTED: all fourteen below KILLED, each on the assertion registered for it in advance,",
    "in one run in a detached copy at 3b0b4968 with the CLI rebuilt before every mutation, against a",
    "baseline of 78 progress marks. Marks reached, in file order: 19, 17, 77, 77, 75, 75, 72, 77, 76,",
    "73, 77, 75, 76, 77. The two lowest are the two that restore the shipped defect and truncate the",
    "suite, which is why they carry their own completion marker. Earlier full runs of this set graded",
    "seven of ten and then ten of ten, and every non-kill in them was a fault in the SET rather than in",
    "the code it grades: two went red on their named assertion and were refused for never reaching a",
    "marker they cannot reach, and one was registered on an assertion that stays GREEN under it, then",
    "re-registered on the one measured to redden.",
    "",
    "THE SURVIVOR THIS FILE USED TO RECORD IS GONE, and how it went is the point. Deleting `if",
    "(reconnect) ownStdin()` from the session `finally` left every cell green, and the honest reason",
    "was that this harness could not produce the fault the line exists for: a session that ends",
    "transport-class while the SOCKET is still alive, where the hand-back's flush actually runs.",
    "Review then read the line's POSITION and found it worse than ungradable, because it sat after",
    "the flush and the close, each bounded by the link deadline, so the window its own comment",
    "claimed to close was over before it ran. The line moved to the top of the `finally`, and the",
    "missing instrument was built rather than the gap documented: the proxy reads NATS framing and",
    "drops whole MSG frames while passing PING, PONG and INFO, which raises a `gap` on a connection",
    "nothing has closed. Deleting the line still does not LOSE a press, it delays it, so the cell",
    "that grades it presses detach and then one more key, because buffered bytes come back in ONE",
    "read and the exact-match test then refuses them. An ungradable line was a fact about the",
    "instrument set, not about the line.",
    "",
    "A STATED LIMITATION OF THE EXACT-MATCH TEST, and deliberately not a mutation here. Both readers",
    "require the detach byte to arrive alone in a read, and a terminal running the kitty keyboard",
    "protocol or xterm's `modifyOtherKeys` encodes one press as a multi-byte CSI sequence that no",
    "length-one test can match. Filed as #598 rather than changed here: both protocols are opt-in and",
    "off by default, the session reader has carried the test since long before this change, and any",
    "replacement still has to refuse a chunk in which the byte merely appears alongside other input,",
    "which is what cells F and G grade.",
    "",
    "ONE MUTATION SURVIVES THIS SUITE AND IS GRADED IN THE SIBLING ONE, and the paragraph that used to",
    "stand here was an excuse. Deleting the `takeStdin?.()` handoff from `attachClient`'s `onReady`",
    "leaves BOTH readers installed for the whole session, and this suite stays green under it: with",
    "both live a lone detach byte still ends the attach, and a chunk carrying the byte alongside",
    "other input is still refused by the same exact-match test on either side, so cells F, G and H",
    "cannot see the difference. This file used to conclude from that green that the reachable",
    "difference was `a press landing in the instant the link dies`, a class it said the harness does",
    "not build machinery for. That was wrong, and review proved it wrong by running it rather than by",
    "arguing: with the handoff deleted, a detach pressed in the ORDINARY backoff wait after a session",
    "that WAS ready is dead, measured at 15075ms stuck with no detach against 100ms on the shipping",
    "build. The mechanism is the release, not the read. `ownStdin` is `idle ??= watchDetachKey(...)`,",
    "so a watcher that was never stopped is neither reinstalled nor RESUMED, while `cleanup` pauses",
    "the stream the session did take. This suite never presses detach in that window, so its green is",
    "true of the suite and silent about the code.",
    "",
    "The gap is closed where the window already lives rather than by adding a cell here.",
    "`smoke:attach-reconnect` cell B presses detach DURING a reconnect, which is exactly that window,",
    "and the mutation is registered against it in `bin/smoke/mutations/attach-reconnect.json`:",
    "measured at f345809a against a green baseline of 72, the deletion gives 67 passed and 5 failed,",
    "first red on `the detach key ends the attach mid-reconnect`, then the clean-exit and",
    "held-session lines, then cell J's `reconnectsAtPress 0` against `now 1` and `pressToExitMs",
    "27172` against a boundary 26826ms away. So the mutant is covered ON CI, by the suite whose cell",
    "enters the window, and the earlier enumeration was right about which two suites reach the line",
    "at runtime and wrong about which one grades it: attach.smoke.ts drives the key parser on",
    "hand-built input and attach-repaint drives the manager's own face.",
    "",
    "MUTATION 14 IS THE LAST DEFECT THIS LANE FOUND, and how it was found belongs here. Review read",
    "`attachClient`'s cleanup and named a window no cell entered: a session that ends BEFORE its",
    "`ready` fires, where the handoff never runs, the loop's own reader is still installed, and the",
    "cleanup pauses the stream anyway. Four rounds of cutting one link ever more precisely AT",
    "ESTABLISHMENT could not produce it, and this file briefly recorded it as unreachable. That was",
    "wrong. `ready` is a flush, a flush is a PING and its PONG, so the window is ONE ROUND TRIP wide,",
    "and on healthy loopback that is a fraction of a millisecond: the trigger was not too blunt, the",
    "window was too short. Cell O slows the link in both directions to widen it to about 800ms and",
    "drops the client's own chunk carrying the rail SUB and the flush PING, so no PONG for that flush",
    "can exist and ready provably has not fired. Measured with no fix in the build: 76 passed, 1",
    "failed, the red on the delivery assertion with the nonce in its payload. With the fix: 77/0. The",
    "cell's FIRST shape passed 77 of 77 against the same defective code, because it left the link",
    "healed after the cut and the next session's `ready` resumed the very stream whose pause was the",
    "defect: a trigger that leaves the system able to repair itself measures the repair."
  ],
  "mutations": [
    {
      "cell": "A. bytes typed while the loop is WAITING never reach the agent",
      "name": "the wait runs with the reader released, so what is typed between attempts buffers and flushes",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "      const watch = ownStdin();",
      "replace": "      releaseStdin(); const watch = undefined as ReturnType<typeof ownStdin>;",
      "expectRed": "...and the seat read nothing of what was typed at a terminal with no session",
      "completionMarker": "A: the manager is back on the session count it started with",
      "note": "INVENTED rather than restored, and the only entry here of that kind, so it is labelled instead of blending in. Checked against history rather than against the comment above it: at `a7742a75^` the backoff wait ALREADY ran `const watch = watchDetachKey(key.byte)`. The wait was always owned, and what that commit added was ownership of the ESTABLISHMENT window and the gaps between attempts. So cell A guards a regression this codebase has never had and there is no shipped defect to put back. The only shape that reds it is an unowned wait, and removing the wait's reader also removes the detach key from every wait, so the run cascades and ends at 19 of 84 marks. Hence the per-entry completionMarker: this mutant is meant to survive as far as cell A and no further. Graded against the suite-END marker instead it reads INCONCLUSIVE, which is a fact about which marker was used and not about the cell. The anchor is CODE ONLY and deliberately stops before the trailing comment: a `find` that spans prose is disarmed by anyone tidying prose, and `smoke:mutation-fixtures` rejects one that does. Two earlier candidates proved nothing and are named so they are not retried: `const watch = undefined` alone does not compile (TS2339, `watch` narrows to `never`), and dropping the `ownStdin()` call without releasing removes no reader at all, because `ownStdin` is idempotent through `idle ??=` and the reader is installed before the loop."
    },
    {
      "name": "the fix reverted for exactly the establishment window: the reader is stopped before each attempt",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "    const attempting = establishAttachSession(vv, pin, reconnect, first);",
      "replace": "    releaseStdin();\n    const attempting = establishAttachSession(vv, pin, reconnect, first);",
      "expectRed": "...and the seat read nothing of what was typed while the session was being established",
      "cell": "C. bytes typed during an attempt that SUCCEEDS never reach the agent",
      "note": "The shipped defect restored, in the shape it actually had: the old code stopped the watcher and paused stdin before each attempt, so bytes typed while a session was being established buffered and were flushed into the seat by `attachClient`'s own `stdin.resume()`. `releaseStdin()` rather than `idle?.stop()` on purpose, so the next wait builds a FRESH reader exactly as the old code did: a mutant that leaves a stopped reader in place is not the old behaviour, and it would redden cells A and B, which the old code passed. MEASURED at the tip it grades, by hand, with the suite's full output captured rather than inferred from a mark count: A and B stay GREEN, C reddens on the assertion named here with the nonce visible in the seat's own sink, and D reddens twice, on the recorded SIGINT and on the seat's pid being gone. E, F and G are then UNREACHED rather than green: the mutant's own defect killed the agent, so the next cell's attach refuses with `no agent \"stdinseat\"` and the suite ends there. Unreached is not passed, and the boundary claim above rests on A and B, which did run.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build",
      "completionMarker": "C: the manager is back on the session count it started with"
    },
    {
      "cell": "D. Ctrl-C typed in that window does not reach the agent as a signal",
      "name": "the same establishment-window revert, asked whether that window turns a 0x03 into a real signal",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "    const attempting = establishAttachSession(vv, pin, reconnect, first);",
      "replace": "    releaseStdin();\n    const attempting = establishAttachSession(vv, pin, reconnect, first);",
      "expectRed": "...the agent recorded no signal",
      "completionMarker": "D: the manager is back on the session count it started with",
      "note": "The SAME one-line revert the C entry uses, registered a second time because the claim is different and cannot be inferred from C's. C asks whether DATA typed in the establishment window reaches the agent. D asks whether a 0x03 typed in that same window arrives as a real SIGNAL, and the answer runs through the pty's line discipline rather than through the byte itself. That distinction was predicted wrong once in this branch's own review, on the reasoning that a raw 0x03 handed to a stub is just data: the seat runs under a pty, the line discipline turns it into a SIGINT, and the seat dies. Measured here, the sink carries `<SIGINT>` and the pid is gone.\nIts red KILLS THE SEAT, which is what D exists to forbid, so the run cannot go on for long: cell E throws `could not resolve \"stdinseat\": no agent` because the seat E needs is the one this mutant killed. The cascade lands on E and NOT on D, which is why the per-entry completionMarker is D's own settle line. That marker prints at mark 24, three marks and two passing checks after the expectRed at mark 21, so it qualifies the red rather than restating it. Do not shorten it to the expectRed: a marker satisfied by the very red it is meant to qualify proves nothing."
    },
    {
      "name": "the wait's reader is stopped when the wait ends, so an attempt runs with nobody reading",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "      const detached = watch ? await Promise.race([waited, watch.pressed.then(() => true)]) : await waited;\n      clearTimeout(timer);",
      "replace": "      const detached = watch ? await Promise.race([waited, watch.pressed.then(() => true)]) : await waited;\n      clearTimeout(timer);\n      watch?.stop();",
      "expectRed": "...and the seat read nothing of what was typed during the failed attempt",
      "cell": "B. bytes typed during an attempt that FAILS never reach the agent",
      "note": "main's own line, restored: the old code stopped the wait's watcher when the wait ended, so every attempt after it ran with nobody reading. Cell B was filed as a boundary on the belief that a failed attempt is always followed by a wait whose watcher covers what was typed into it. Measurement says otherwise: an establishment on a held link runs to the 5s link deadline while the cell holds for 2.5s, so the bytes go into an attempt that is still in flight, and the wait that would have covered them has not started. The cell now waits for the client's own next dial before it grades, which is what turns this line from a boundary into a registered kill.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build",
      "completionMarker": "B: the manager is back on the session count it started with"
    },
    {
      "name": "the detach key is not read while a session is being established",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "        idle.pressed.then(() => ({ detached: true }) as const),",
      "replace": "        new Promise<{ readonly detached: true }>(() => {}),",
      "expectRed": "...without announcing the session that landed behind it",
      "cell": "E. the detach key struck in that same window ends the attach and hands the slot back",
      "note": "What the race arm buys, stated as the assertion that actually moves. Deleting it does not lose the press: the handoff answer in `attachClient` still closes the transport once the session opens, so the attach still ends and still exits clean, which is why cell E's exit check stayed GREEN under this mutation and the kill first registered here was the wrong one. What changes is WHEN, and the operator-visible cost of the delay: the loop announces `[cotal: reconnected]` for a session nobody is attached to any more, then detaches. Re-registered on the assertion measured to redden, one red check in 57, reconnectsAtPress 0 against 1 at the check.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "a session that lands behind the press is forgotten instead of handed back",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "          abandoned.push({ grant: late.grant, creds: late.creds, inbox: late.inbox, server: late.server });",
      "replace": "",
      "expectRed": "...and the manager is back on the session count it started with, with no slot left behind",
      "cell": "E. the detach key struck in that same window ends the attach and hands the slot back",
      "note": "Ownership, graded on the manager's own live-session count rather than on anything the client says. The seat this suite uses is SILENT, so nothing on the serving side reaps that session for us: the count coming back to base is the client's hand-back or nobody's. This is the assertion that makes the late-landing session a slot returned rather than a slot leaked.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the between-sessions reader SCANS for the detach byte instead of matching one alone",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "  const onData = (d: Buffer) => { if (d.length === 1 && d[0] === byte) hit(); };",
      "replace": "  const onData = (d: Buffer) => { if (d.includes(byte)) hit(); };",
      "expectRed": "the attach is still up: the chunk is not a keypress",
      "cell": "F. a detach byte sharing a read with another byte is not a detach, and is not delivered",
      "note": "The exact-match decision, stated as a mutation. A chunk carrying 0x1d alongside anything else is a paste or a reader that was not reading, never a keypress: measured on a pty, a real keypress arrives in a read of its own even at 3ms spacing. Widening the test to a scan makes a paste end someone's attach, and the cell catches it because the attach exits where it should have stayed up.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the session's own reader SCANS for the detach byte instead of matching one alone",
      "file": "implementations/cli/src/lib/attach-client.ts",
      "find": "      if (d.length === 1 && d[0] === detach.byte) {",
      "replace": "      if (d.includes(detach.byte)) {",
      "expectRed": "...and the attach is still up",
      "cell": "G. the same chunk in a LIVE session is data, forwarded to the agent, and still not a detach",
      "note": "The same decision on the other side of the handoff, and the cost of it: in a LIVE session a chunk containing 0x1d is DATA and goes to the agent with the byte in it. Widening the test here turns that paste into a detach. Both readers are mutated separately on purpose: one test passing is not evidence about the other, and they are in different packages.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the handoff swallows a press that landed while the session was opening",
      "file": "implementations/cli/src/lib/attach-client.ts",
      "find": "      if (takeStdin?.()) {\n        transport.close();\n        return;\n      }",
      "replace": "      takeStdin?.();",
      "expectRed": "the press ends the attach, though it landed while the session was opening",
      "cell": "H. the detach key pressed while the new session is OPENING still detaches",
      "note": "Restores the swallow review found: the caller's reader owns stdin until the handoff, the handoff runs when the session is READY, and a press in between is seen by that reader and awaited by nobody. This mutation is also cell H's PREMISE CHECK: with the swallow back, the cell can only pass if the press landed after ready, which is not the window it is aiming at, so the kill is what proves the aim. It needs the suite's slow link to be reachable at all: on loopback the window is under a millisecond wide.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the first attach does not own the keyboard, so what is typed before it comes up is flushed into the seat",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "  if (reconnect) ownStdin();\n  for (;;) {",
      "replace": "  for (;;) {",
      "expectRed": "...and the seat read nothing of what was typed before it came up",
      "cell": "I. bytes typed before the FIRST attach comes up never reach the agent either",
      "note": "The reader used to be installed only from the first RECONNECT onwards, which left the commonest path of all uncovered: an attach that has not come up yet is an attach with no session, and everything typed at the terminal while the first establishment ran buffered in the paused stream and was flushed into the seat by the session's own resume. Deleting the line restores exactly that. This mutation must leave cell J green: a pipe is not supposed to have a reader, and a mutation that reddened both would be measuring the gate's existence rather than its position.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the population gate is dropped, so a script's input is eaten wherever the loop installs the reader",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "  const ownStdin = (): typeof idle => (process.stdin.isTTY ? (idle ??= watchDetachKey(key.byte)) : undefined);",
      "replace": "  const ownStdin = (): typeof idle => (idle ??= watchDetachKey(key.byte));",
      "expectRed": "the agent receives what the script wrote",
      "cell": "J. with stdin a PIPE, what was written before the session opened still arrives",
      "note": "The population gate, mutated where it now LIVES rather than where it used to be written. It moved inside `ownStdin` because there are three call sites and a gate at one of them is a promise the other two break: with it at the first attach only, a piped attach buffered correctly until its first reconnect and then had the reader installed on it by the backoff, which is cell L. Dropping the gate reddens J first because J runs first; the same mutation is what cell L would catch one window later, which is the point of putting the gate in one place.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the keyboard is unowned while a faulted session is handed back",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "      if (reconnect) ownStdin();",
      "replace": "",
      "expectRed": "the press ends the attach, though it landed while the session was being handed back",
      "cell": "K. a detach pressed while a FAULTED session is being HANDED BACK is still a keypress",
      "note": "The moved line, deleted rather than moved back, because the suite could not tell those two apart until the instrument existed. Cell K faults a session over a link that is still UP: the proxy drops whole MSG frames while passing PING, PONG and INFO, so the rail raises `gap` on a connection nothing has closed and the hand-back's flush actually runs to its deadline. Deleting the line does not LOSE the press, it delays it, which is why the cell presses detach and then one more key: the buffered bytes come back in one read, and the exact-match test refuses a chunk with company, so the attach that should have detached is still up.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the gate is applied at the first attach only, so a pipe gains a dropper when it reconnects",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "(process.stdin.isTTY ? (idle ??= watchDetachKey(key.byte)) : undefined)",
      "replace": "(process.stdin.isTTY || !first ? (idle ??= watchDetachKey(key.byte)) : undefined)",
      "expectRed": "...and the seat receives what the script wrote while there was no session",
      "cell": "L. with stdin a PIPE, what is written while there is NO session still arrives",
      "completionMarker": "L: the manager is back on the session count it started with",
      "note": "The defect exactly as it shipped inside this branch's own review round, restored in one line: the gate honoured on the FIRST attach and ignored everywhere after it, which is what a gate written at one of three call sites amounts to. Cell J stays GREEN, because the first open still buffers a pipe; cell L reddens, because the backoff then installs a reader on that same pipe and reads the script's bytes into the dropper. That J-green-L-red pair IS the finding, and it is why the gate lives in one place now. Measured before the fix: 62 passed, 1 failed, the single red this assertion, with the seat's read empty.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the exit path leaves the process's own claim on stdin, so a piped attach never gives the shell back",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "    if (typeof process.stdin.unref === \"function\") process.stdin.unref();\n",
      "replace": "",
      "expectRed": "...and the detach key still ends a piped attach",
      "cell": "J. with stdin a PIPE, what was written before the session opened still arrives",
      "completionMarker": "...leaving the manager on the session count it started with",
      "note": "THE AIM MOVED, and it moved to a cell named by the mutation's own output rather than by argument. Registered first on cell M, the piped one-shot, this arm came back WRONG-RED: red, but not on that line, and it had reached only 61 of 67 marks, so it had ended before M ran. Re-measured alone at this tip with the full suite output captured, deleting the release reddens THREE checks, in this order: cell J's `...and the detach key still ends a piped attach`, cell L's exit check, and cell M's. All three are exit checks on a route where stdin is a PIPE, which is exactly the stream kind that has an `unref` to leave behind, and cell N, whose stdin is a FILE, stays GREEN under the same mutation: the release is missed precisely where there is something to release. The earliest red is registered because a truncation cannot hide it, and cell J's own trailer is the completion marker for the same reason. What this proves is unchanged: without that line the command prints `detached from` and never gives the shell back. The other half of the claim, that the hang is pre-existing on the one-shot piped route rather than introduced here, is not this mutation's to make and is measured on the build that ships: `pnpm probe:pipe-oneshot` reports exit=HANG ms=>30000 at origin/main a8589c11 and exit=0 ms=102 at this tip, both with the detach honoured and the nonce delivered. Measured on the lingering process itself with an unref'd-timer preload: `getActiveResourcesInfo()` reported three PipeWraps and nothing else, no timers and no sockets, after `detached from` had already printed. A pause alone was measured and was not enough.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "the exit release is called on a stream kind that has no `unref`, so a file-backed attach crashes on the way out",
      "file": "implementations/cli/src/commands/agents.ts",
      "find": "if (typeof process.stdin.unref === \"function\") process.stdin.unref();",
      "replace": "process.stdin.unref();",
      "expectRed": "N: ...and never crashed releasing a stream that has nothing to release",
      "cell": "N. an attach whose stdin is a FILE exits without crashing on the way out",
      "completionMarker": "N: the manager is back on the session count it started with",
      "note": "The shipped defect in one line. Dropping the capability test restores the unguarded call, and a file-backed stdin is an `fs.ReadStream` with no `unref`, so `done` throws `process.stdin.unref is not a function` on the way out. Registered on the check that names that text rather than on the exit code, because an exit code says only that the route is broken and this cell exists to say HOW. Found by CI in `smoke:cli-on-instance`, which spawns attach with stdin ignored for an unrelated claim; every cell in this file drove the attach through a pty or a pipe, the two kinds that HAVE `unref`, so the gate had no cell in the universe where the bug lived.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    },
    {
      "name": "a session that never took the stream pauses it anyway on the way out",
      "file": "implementations/cli/src/lib/attach-client.ts",
      "find": "else if (tookStdin) stdin.pause();",
      "replace": "else stdin.pause();",
      "expectRed": "...and the seat read nothing of what was typed after a session died before it was ready",
      "cell": "O. a session that dies BEFORE it is READY leaves the keyboard where it was",
      "note": "The shipped defect in one word, and it restores the code exactly as it was before this round: `cleanup` pauses the stream whether or not this session ever resumed it. A session that ends before its `ready` fires never ran the handoff, so the loop's own watcher is still installed, and pausing under it leaves a reader that reads nothing for the whole backoff and every attempt after it; the bytes come back in the next session's `resume` and go to the agent. Measured on this exact code before the fix line existed: 76 passed, 1 failed, the red on this assertion with the nonce in the payload and `sessionsSinceTheCut: 1`.",
      "afterRestore": "pnpm --filter @cotal-ai/cli build"
    }
  ]
}
